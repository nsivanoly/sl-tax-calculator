# Database password (used by docker-compose for both PostgreSQL and backend)
DB_PASSWORD=taxpass123

# Seed mode: what data to load on a fresh database
#   sample  — demo users Alice & Bob (default)
#   user    — your data from user_data/users.json
#   both    — sample users + your data
#   none    — empty database
SEED_MODE=sample
